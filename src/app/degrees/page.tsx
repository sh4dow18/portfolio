// Degrees Page Requirements
import DegreesPage from "@/features/degrees/page";
import { DEGREES_METADATA } from "./metadata";
// Degrees Page Metadata
export const metadata = DEGREES_METADATA;
// Degrees Page Main Function
function Degrees() {
  return <DegreesPage />;
}

export default Degrees;
