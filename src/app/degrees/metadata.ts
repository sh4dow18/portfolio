// Degrees Metadata Requirements
import { ROUTES_MAP } from "@/shared/config/routes";
import type { Metadata } from "next";
// Degrees Metadata
export const DEGREES_METADATA: Metadata = {
  title: ROUTES_MAP["degrees"].title,
};
