// Experience Metadata Requirements
import { ROUTES_MAP } from "@/shared/config/routes";
import type { Metadata } from "next";
// Experience Metadata
export const EXPERIENCE_METADATA: Metadata = {
  title: ROUTES_MAP["experience"].title,
};
